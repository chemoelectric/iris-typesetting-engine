# Iris TeX TRIP Sub-Module (`iris_tex_trip`)

## Overview
The `iris_tex_trip` sub-module encapsulates Donald Knuth's TRIP diagnostic benchmark suite for TeX implementation validation.

## API Procedures

### `tex_run_trip_suite(status, report_msg)`
Executes the complete TRIP test suite and generates a diagnostic report.
