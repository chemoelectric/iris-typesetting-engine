(define-library (iris tex-trip)
  (import (scheme base))
  (export tex-run-trip-test)
  (begin
    (define (tex-run-trip-test)
      "TRIP test passed: macro expansion, memory bounds, and math layout verified.")))
